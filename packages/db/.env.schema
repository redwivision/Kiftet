# @import(../../apps/server/, pick=[NODE_ENV, DATABASE_*])
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ---

# ⚠️ PLACEHOLDER — the prototype branch runs on SQLite.
# Path to the local SQLite file, e.g. ./kiftet-dev.db. Ignored on main (Postgres).
# @type=string(minLength=1)
DATABASE_FILE=./kiftet-dev.db